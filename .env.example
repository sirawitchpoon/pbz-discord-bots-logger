# MongoDB (ใช้ DB เดียวกับ honorbot ก็ได้ แค่คนละ collection: action_logs)
MONGO_URI=mongodb://mongodb:27017/honorbot
PORT=3002

# สร้างเอง (เช่น openssl rand -hex 24) แล้วใส่ค่าเดียวกันในบอทอื่นที่ส่ง log + ใน Google Apps Script
API_KEY=your-secret-api-key
